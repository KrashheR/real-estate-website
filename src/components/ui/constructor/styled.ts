import styled from 'styled-components';

export const StyledConstructor = styled.div`
  margin-top: 30px;
  margin-bottom: 30px;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 40px;
`;

export const StyledConstructorItemsList = styled.ul`
  position: relative;
`;

